using UnityEngine;

public class TableBGScript : MonoBehaviour
{
    public SpriteRenderer nightTableCamborne; 
    public SpriteRenderer nightTable; 
    public SpriteRenderer dayTable; 

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
